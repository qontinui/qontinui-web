{
  "$comment": [
    "Style-gate invariant spec for /library (the Step Library category grid).",
    "Element ids verified against the clean authed capture (run 26985959070). Only",
    "POSITIONED, always-rendered elements are asserted on (missing/no-bbox ids",
    "HARD-FAIL): the six category-card links carry a title-derived slug for a FIXED,",
    "hard-coded category set (Playwright Tests / Tests / Checks / Shell Commands /",
    "Contexts / Prompt Snippets), and the left-nav controls (button-search-k,",
    "button-home-0) are the cross-route app shell — both stable.",
    "",
    "contrast_meets_wcag is on button-search-k only (the one element with declared",
    "fg+bg colors on this route; sampled-color elements would fail spuriously)."
  ],
  "assertions": [
    { "type": "no_clipping" },
    { "type": "no_overlap", "elements": ["link-playwright-tests-0-playwright--0", "link-tests-0-automated-tests-includ-1"] },
    { "type": "no_overlap", "elements": ["link-playwright-tests-0-playwright--0", "link-shell-commands-0-reusable-shel-3"] },
    { "type": "no_overlap", "elements": ["button-search-k", "button-home-0"] },
    { "type": "text_fits_container", "element": "button-search-k" },
    { "type": "contrast_meets_wcag", "element": "button-search-k", "level": "aa" },
    { "type": "no_layout_shift_since", "baseline": "library" }
  ]
}

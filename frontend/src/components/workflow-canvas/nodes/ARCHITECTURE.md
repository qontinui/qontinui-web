# Node System Architecture

Visual architecture diagrams for the workflow canvas node system.

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Workflow Canvas Node System                      │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
         ┌──────────────┐  ┌─────────────┐  ┌──────────────┐
         │  Components  │  │  Utilities  │  │   Registry   │
         └──────────────┘  └─────────────┘  └──────────────┘
                │                  │                 │
        ┌───────┼───────┐         │          ┌──────┴──────┐
        │       │       │         │          │             │
        ▼       ▼       ▼         ▼          ▼             ▼
    BaseNode  Handles Icons  node-utils  NODE_TYPES  Metadata
        │
        └─────────┬─────────┬─────────┬─────────┐
                  │         │         │         │
                  ▼         ▼         ▼         ▼
          ControlFlow   GuiAction  DataOps  SpecialNodes
            (6 types)   (18 types) (8 types) (7 types)
```

## Component Hierarchy

```
BaseNode (Foundation)
├── Props
│   ├── action: Action<T>
│   ├── executionState: 'idle' | 'running' | 'completed' | 'failed' | 'skipped'
│   ├── showInputHandle: boolean
│   ├── showOutputHandle: boolean
│   └── outputHandleIds: string[]
│
├── Features
│   ├── Header (icon + type + name)
│   ├── Body (summary + error + badges)
│   ├── Input handle (left)
│   ├── Output handle(s) (right)
│   ├── Execution state indicator
│   └── Selection highlighting
│
└── Variants
    ├── MultiOutputNode (IF, LOOP, SWITCH, TRY_CATCH)
    │   └── outputLabels: { id, label, color }[]
    ├── CompactNode (WAIT, GET_VARIABLE)
    │   └── Reduced padding & size
    └── TerminalNode (START, END)
        └── Special styling + variant prop
```

## Node Categories & Types

```
┌─────────────────────────────────────────────────────────────┐
│                      30+ Action Types                        │
└─────────────────────────────────────────────────────────────┘
                                │
                    ┌───────────┼───────────┐
                    │           │           │
        ┌───────────▼───────┐   │   ┌───────▼───────┐
        │  Control Flow (6) │   │   │  GUI Actions  │
        │   Blue/Purple     │   │   │   (18 types)  │
        │                   │   │   └───────────────┘
        │  IF, LOOP, SWITCH │   │           │
        │  TRY_CATCH, BREAK │   │   ┌───────┼───────┐
        │  CONTINUE         │   │   │       │       │
        └───────────────────┘   │   ▼       ▼       ▼
                                │ Mouse  Keyboard  Find
                                │  (8)     (5)     (5)
                                │ Green    Cyan   Amber
                    ┌───────────▼───────┐
                    │  Data Operations  │
                    │    (8 types)      │
                    │     Orange        │
                    │                   │
                    │  SET_VAR, FILTER  │
                    │  MAP, SORT, etc.  │
                    └───────────────────┘
                                │
                    ┌───────────▼───────┐
                    │  Special Nodes    │
                    │    (7 types)      │
                    │   Various Colors  │
                    │                   │
                    │  START, END, etc. │
                    └───────────────────┘
```

## Handle System

```
Standard Node (Single Output)
┌─────────────────────┐
│                     │
●  Node Content       ● main
│                     │
└─────────────────────┘
input                 output


IF Node (Dual Output)
┌─────────────────────┐
│                     │
●  IF condition       ● true
│                     │
│                     ● false
└─────────────────────┘
input                 outputs


LOOP Node (Dual Output)
┌─────────────────────┐
│                     │
●  LOOP 5 times       ● loop (back to start)
│                     │
│                     ● main (exit)
└─────────────────────┘
input                 outputs


SWITCH Node (Multi Output)
┌─────────────────────┐
│                     │
●  SWITCH value       ● case_0
│                     │
│                     ● case_1
│                     │
│                     ● case_2
│                     │
│                     ● default
└─────────────────────┘
input                 N+1 outputs


Terminal Node (No Output)
┌─────────────────────┐
│                     │
●  BREAK/CONTINUE     │
│                     │
└─────────────────────┘
input only
```

## Execution State Flow

```
State Transitions:
┌──────┐
│ idle │
└──┬───┘
   │ workflow starts
   ▼
┌─────────┐
│ running │ ← Blue pulsing animation
└──┬──┬───┘
   │  │
   │  └─── error ──→ ┌────────┐
   │                 │ failed │ ← Red border + X badge
   │                 └────────┘
   │
   └─── success ──→ ┌───────────┐
                    │ completed │ ← Green border + ✓ badge
                    └───────────┘

Special State:
┌─────────┐
│ skipped │ ← 50% opacity (conditional skip)
└─────────┘
```

## Visual State Indicators

```
Idle State:
┌─────────────────────┐
│ 🖱️  CLICK           │
│                     │
│ Click "Submit"      │
└─────────────────────┘

Running State:
  ╭───────────────────╮  ← Pulsing blue ring
  │┌─────────────────┐│
  ││ 🖱️  CLICK    ●  ││  ← Blue dot
  ││                 ││
  ││ Click "Submit"  ││
  │└─────────────────┘│
  ╰───────────────────╯

Completed State:
┏━━━━━━━━━━━━━━━━━━━━━┓  ← Green border
┃ 🖱️  CLICK         ✓ ┃  ← Checkmark badge
┃                     ┃
┃ Click "Submit"      ┃
┗━━━━━━━━━━━━━━━━━━━━━┛

Failed State:
┏━━━━━━━━━━━━━━━━━━━━━┓  ← Red border
┃ 🖱️  CLICK         ✕ ┃  ← X badge
┃                     ┃
┃ Error: Not found    ┃  ← Error message
┗━━━━━━━━━━━━━━━━━━━━━┛

Skipped State:
░░░░░░░░░░░░░░░░░░░░░░░  ← 50% opacity
░ 🖱️  CLICK           ░
░                     ░
░ Click "Submit"      ░
░░░░░░░░░░░░░░░░░░░░░░░
```

## Category Color Scheme

```
Find Actions (Amber #fbbf24)
┌─────────────────────┐
│ 🔍 FIND             │  ← Amber header gradient
├─────────────────────┤
│ Find element        │
└─────────────────────┘

Mouse Actions (Green #10b981)
┌─────────────────────┐
│ 🖱️  CLICK           │  ← Green header gradient
├─────────────────────┤
│ Click "Button"      │
└─────────────────────┘

Keyboard Actions (Cyan #06b6d4)
┌─────────────────────┐
│ ⌨️  TYPE            │  ← Cyan header gradient
├─────────────────────┤
│ Type "Hello"        │
└─────────────────────┘

Control Flow (Blue #3b82f6)
┌─────────────────────┐
│ 🔀 IF               │  ← Blue header gradient
├─────────────────────┤
│ If (condition)      │
└─────────────────────┘

Data Operations (Orange #f97316)
┌─────────────────────┐
│ 📊 SET_VARIABLE     │  ← Orange header gradient
├─────────────────────┤
│ Set counter         │
└─────────────────────┘

State Actions (Indigo #6366f1)
┌─────────────────────┐
│ ⚡ GO_TO_STATE      │  ← Indigo header gradient
├─────────────────────┤
│ → LoginScreen       │
└─────────────────────┘
```

## Data Flow

```
Workflow Definition
      │
      ▼
┌───────────────┐
│ Action Schema │ (TypeScript types)
└───────┬───────┘
        │
        ▼
┌───────────────────┐
│  Node Component   │
│   Selection       │
└───────┬───────────┘
        │
    ┌───┴───┐
    │       │
    ▼       ▼
┌────────┐ ┌─────────────┐
│ Config │ │ Execution   │
│ Props  │ │ State       │
└───┬────┘ └──────┬──────┘
    │             │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │ BaseNode    │
    │ Rendering   │
    └─────────────┘
           │
    ┌──────┼──────┐
    │      │      │
    ▼      ▼      ▼
 Header  Body  Handles
```

## Registry Lookup Flow

```
React Flow Rendering
        │
        ├── Node type: 'CLICK'
        │
        ▼
┌────────────────┐
│ NODE_TYPES     │ (Registry lookup)
│   ['CLICK']    │
└────────┬───────┘
         │
         ├── Returns: ClickNode component
         │
         ▼
┌────────────────┐
│  ClickNode     │
└────────┬───────┘
         │
         ├── Uses BaseNode
         │
         ▼
┌────────────────┐
│  BaseNode      │
└────────┬───────┘
         │
         ├── Calls getNodeIcon('CLICK')
         │   Calls getNodeSummary(action)
         │   Calls getOutputHandleIds(action)
         │
         ▼
    Rendered Node
```

## File Dependencies

```
index.ts (Main export)
    │
    ├── BaseNode.tsx
    │   ├── handles.tsx
    │   ├── node-icons.tsx
    │   └── node-utils.ts
    │
    ├── ControlFlowNodes.tsx
    │   ├── BaseNode.tsx
    │   └── handles.tsx
    │
    ├── GuiActionNodes.tsx
    │   └── BaseNode.tsx
    │
    ├── DataOperationNodes.tsx
    │   └── BaseNode.tsx
    │
    ├── SpecialNodes.tsx
    │   └── BaseNode.tsx
    │
    ├── node-registry.ts
    │   ├── ControlFlowNodes.tsx
    │   ├── GuiActionNodes.tsx
    │   ├── DataOperationNodes.tsx
    │   └── SpecialNodes.tsx
    │
    ├── node-utils.ts
    │   └── action-types.ts
    │
    ├── node-icons.tsx
    │   ├── lucide-react
    │   └── action-types.ts
    │
    ├── handles.tsx
    │   ├── @xyflow/react
    │   └── action-types.ts
    │
    └── nodes.css
```

## Integration Points

```
┌──────────────────────────────────────────────┐
│           React Flow Canvas                   │
│  (Agent 16's implementation)                  │
└────────────────┬─────────────────────────────┘
                 │
                 ├── nodeTypes={NODE_TYPES}
                 │
                 ▼
┌──────────────────────────────────────────────┐
│         Custom Node Components                │
│    (This implementation - Agent 17-19)        │
└────────────────┬─────────────────────────────┘
                 │
                 ├── Uses Action types
                 │
                 ▼
┌──────────────────────────────────────────────┐
│          Action Schema                        │
│  (/lib/action-schema/action-types.ts)        │
└──────────────────────────────────────────────┘
```

## Performance Optimization

```
React Flow Rendering
        │
        ├── Viewport culling (built-in)
        │   └── Only renders visible nodes
        │
        ├── Node memoization
        │   └── React.memo() on components
        │
        ├── Handle positioning
        │   └── Pre-calculated positions
        │
        └── CSS animations
            └── GPU-accelerated transforms
```

## Accessibility

```
Keyboard Navigation
    │
    ├── Focus management
    │   └── .workflow-node:focus
    │
    ├── ARIA labels
    │   └── Descriptive node content
    │
    └── Screen reader support
        └── Semantic HTML

Visual Accessibility
    │
    ├── High contrast mode
    │   └── @media (prefers-contrast: high)
    │
    ├── Reduced motion
    │   └── @media (prefers-reduced-motion)
    │
    └── Dark mode
        └── @media (prefers-color-scheme: dark)
```

## Extension Points

```
Adding New Node Type
        │
        ├── 1. Define in action-types.ts
        │      export type ActionType = ... | 'NEW_TYPE'
        │
        ├── 2. Create component
        │      export function NewTypeNode(props) { ... }
        │
        ├── 3. Register in node-registry.ts
        │      NODE_TYPES['NEW_TYPE'] = NewTypeNode
        │
        ├── 4. Add icon
        │      ACTION_ICONS['NEW_TYPE'] = IconComponent
        │
        ├── 5. Add summary logic
        │      case 'NEW_TYPE': return '...'
        │
        └── 6. Add styling (optional)
               .new-type-node { ... }
```

## Summary

This architecture provides:

- ✅ **Modularity** - Separated concerns (components, utils, registry)
- ✅ **Extensibility** - Easy to add new node types
- ✅ **Type Safety** - Full TypeScript coverage
- ✅ **Performance** - Optimized rendering
- ✅ **Accessibility** - WCAG compliance
- ✅ **Maintainability** - Clear structure and documentation

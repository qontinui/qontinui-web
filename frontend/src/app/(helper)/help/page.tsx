import { HelperPortal } from "@/components/helper-portal/HelperPortal";

/**
 * /help — the helper-task portal.
 *
 * One task at a time, three big answers. The developer-facing
 * "Help & Documentation" page moved to /help/docs when this route took
 * over /help (helper-task-queue plan Phase 1.4).
 */
export default function HelpPortalPage() {
  return <HelperPortal />;
}

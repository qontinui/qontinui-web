/**
 * Re-export of useTestingState as useTestState for consistent naming.
 * Provides all test case state, execution state, results, and render helpers.
 */
export { useTestingState as useTestState } from "./useTestingState";

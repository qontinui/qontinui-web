name: Secret Scan

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  scan:
    name: Scan for Secrets - Modified
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run secret scanner
        run: echo "Scanning for secrets..."
